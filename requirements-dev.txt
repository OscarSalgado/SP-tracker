# Herramientas de desarrollo/validación (no son dependencias de ningún artículo,
# se instalan a nivel de repo para poder ejecutar scripts/validate_article.py).
ruff==0.16.1
pytest==9.1.1
pytest-cov==7.1.0
pyyaml==6.0.3
nbformat==5.11.1
nbclient==0.11.0
ipykernel==7.3.0

# Para extract_pdf_essence.py: usa pdftotext (apt install poppler-utils) o instala una de:
# - pdfminer.six>=20221105  (Python pure, sin deps system)
# Si hay conflictos de cryptography, usa pdftotext del sistema en su lugar.
